<?xml version="1.0" encoding="utf-8"?>
<ex:WindowEx
    x:Class="VolumeControl.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VolumeControl"
    xmlns:ex="using:WinUIEx"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Basic Volume Control"
    Width="100" Height="140"
    IsResizable="False" IsMaximizable="False">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- Left empty space -->
            <ColumnDefinition Width="Auto" />
            <!-- Content -->
            <ColumnDefinition Width="*" />
            <!-- Right empty space -->
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="20" VerticalAlignment="Center">
            <!-- Volume Icon Button -->
            <Button x:Name="volumeIconButton" Width="70" Height="70" Click="MuteButton_Click"
                Background="Transparent" BorderThickness="0" VerticalAlignment="Center">
                <TextBlock x:Name="volumeIcon" FontFamily="/Assets/Font Awesome 5 Free-Solid-900.otf#Font Awesome 5 Free" 
                       Text="&#xf028;" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Button>

            <!-- Slider -->
            <Slider x:Name="volumeSlider" Width="300" Minimum="0" Maximum="100" Value="50" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>

</ex:WindowEx>
